# R
.Rhistory
.RData
.Rapp.history
.Ruserdata
*.Rproj.user/

# macOS
.DS_Store

# VS Code (keep local editor settings out of the repo)
.vscode/

# renv — the private library is huge; the lockfile is what matters
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/
renv/staging/

# Output that gets regenerated by running the scripts
# (comment these out if you want the CSVs/PNGs versioned)
# output/tables/*.csv
# output/figures/*.png
